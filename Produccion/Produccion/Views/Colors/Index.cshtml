@model IEnumerable<Produccion.Data.Entities.Color>

@{
    ViewData["Title"] = "Index";
}

<h1>Colores</h1>

<p>
    <a asp-action="Create" class="btn btn-outline-primary" style="float:right" >Agregar Nuevo color</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Borrar</a>
            </td>
        </tr>
}
    </tbody>
</table>
