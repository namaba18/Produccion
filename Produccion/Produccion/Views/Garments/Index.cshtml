@model IEnumerable<Produccion.Data.Entities.Garment>

@{
    ViewData["Title"] = "Index";
}

<h1><i class="fa-solid fa-shirt"></i> Prendas</h1>

<p>
    <a asp-action="Create" class="btn btn-dark mb-2" style="float:right">Agregar Nueva Prenda <i class="fa-solid fa-circle-plus"></i></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConsumoInvUnd)
            </th>
            <th>
                Unidades
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConsumoInvUnd)
            </td>
            <td>
                Mts
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id"><i class="fa-solid fa-pen-to-square text-dark"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.Id"><i class="fa-solid fa-trash-can text-dark"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
